# 更新记录

## v0.2.0 — 2026-09-09

- 支持按展示深度追问、候选人主动换项目、连续无有效进展时暂停，以及结束前的开放交流。
- 报告提供能力雷达图、岗位要求矩阵、证据引用和具体提升任务，支持打印 / PDF、Markdown 和 JSON 导出。
- 公共知识库统一为 338 张卡片，使用向量与 BM25 混合召回、自适应 mini 重排、去重和上下文筛选，保留实际引用来源。
- 会话记忆支持从 State 派生摘要、召回早期回答与证据，以及经授权使用的简历全文；最近问答避免重复进入上下文和回答召回结果。
- 简历技能解析在教育、兴趣、游戏经历等章节前停止；前后端共享创建表单校验，返回具体字段及长度限制。
- JD 自动填写在连接中断或临时服务故障时最多重试一次，共用截止时间；失败保留原表单并显示错误类别。
- 追问校验允许项目中的技术术语和单个请求的嵌套判断，返回具体纠错要求，并限制工具校验失败次数。
- 整理架构、开发、知识库与参赛文档；版本源码包含配置样例，排除本地配置、数据库、评测输出与草稿。

升级前先备份数据库。更新代码后运行 `npm ci` 和 `npm run build`，保留原 `.env` 与数据库路径。默认 Demo 无需模型配置，真实模型配置见 [README](README.md)。

## v0.1.0

首个可克隆运行的本地 Demo，包含多项目与可选 JD 输入、Report / Interview Agent 分工、可恢复的 SQLite 会话，以及带原话引用的报告与 Markdown / JSON 下载。
